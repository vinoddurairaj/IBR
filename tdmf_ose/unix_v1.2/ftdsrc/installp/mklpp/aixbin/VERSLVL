#!/usr/bin/ksh
#
# Product Version Level
#
# Derive fix level by looking at $VERSION and then decomposing
#
. ../../../mk/vars.sh

IFS=.; set -- $VERSION
typeset -Z2 VERSION_PART=$1
print ${VERSION_PART}
